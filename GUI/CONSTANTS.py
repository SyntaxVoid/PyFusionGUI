# This file will contain global GUI constants such as font, font size, and colors.
from PyFusionGUI import *
import os

font_name = "Arial"
font = (font_name, 14)

DEFAULT_SETTINGS_DIR = os.path.join(GUI_DIR, ".guiconfig")
